# Server dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=12.0
httpx>=0.25.0
python-dotenv>=1.0.0

# Bridge dependencies (WinRemote MCP integration)
psutil>=5.9.0
Pillow>=10.0.0
pyautogui>=0.9.54
pywin32>=306
tabulate>=0.9.0
thefuzz>=0.22.0

# Optional bridge dependencies
# pytesseract>=0.3.10  # for OCR (also needs Tesseract-OCR installed)
# markdownify>=1.1.0   # for Scrape tool HTML-to-markdown
